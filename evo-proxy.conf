server {
    listen 3000;
    location / {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_buffering on;
        proxy_buffer_size 128k;
        proxy_buffers 8 256k;
    }
}
